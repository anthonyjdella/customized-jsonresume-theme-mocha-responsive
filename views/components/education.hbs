{{#if resume.education.length}}
<div class="container education-container">
    {{> title value="Education" icon="fas fa-user-graduate"}}

    {{#each resume.education}}
        <section class="item">
            {{> section-header-school name=this.institution }}

            <h4 class="display-inline">{{#if studyType}}{{studyType}}{{/if}}{{#and studyType area}} - {{area}}{{/and}}</h4>

            {{#if score}}
            <h5 class="display-inline gpa">GPA {{score}}</h5>
            {{/if}}

            {{#if courses.length}}
            <ul class="two-column">
                {{#each courses}}
                <li>{{{mdToHtml .}}}</li>
                {{/each}}
            </ul>
            {{/if}}
        </section>
    {{/each}}
</div>
{{/if}}
